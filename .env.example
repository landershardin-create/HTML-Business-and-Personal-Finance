# Environment variables required for local dev / deployment
# Do NOT commit real secrets. Set these via environment or repository secrets (GitHub Secrets)
# Replace placeholders below before running or deploy via CI with secrets configured.

AWS_REGION=us-east-1
USE_DYNAMODB=false
JWT_SECRET=__REPLACE_WITH_STRONG_SECRET__
DEV_ADMIN_USER=__REPLACE_WITH_USERNAME__
DEV_ADMIN_PASS=__REPLACE_WITH_PASSWORD__
EMPLOYEES_TABLE=html-biz-payroll-employees
PAYROLL_RUNS_TABLE=html-biz-payroll-payrollruns
KMS_KEY_ARN=
